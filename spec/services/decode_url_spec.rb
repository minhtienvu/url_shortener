module DecodeUrlSpec
  describe "#generate" do
    context "Get a original_url from short_url" do
      it 'Return error if short link is not found in database' do
        ## Return error
      end

      it 'Return original url from short link' do
        ## Return true + original url
      end
    end
  end
end
